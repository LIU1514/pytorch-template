import torch
import numpy as np


def compute_metric(predictionBatch, targetBatch):

    """
    space for documentation
    convert tensors to cpu if required
    convert tensors to numpy arrays if required
    """

    return metricValue

